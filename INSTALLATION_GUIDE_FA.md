# 🚀 راهنمای کامل نصب و اجرای سیستم TSR با XAMPP
## (برای افراد بدون دانش برنامه‌نویسی)

---

## ⭐ مقدمه

این راهنما قدم‌به‌قدم شما را در نصب و اجرای سیستم مدیریت درخواست‌های فنی (TSR) روی کامپیوتر شخصی‌تان با استفاده از نرم‌افزار XAMPP راهنمایی می‌کند. **نیاز به هیچ دانش برنامه‌نویسی ندارید** - فقط کافیست دستورالعمل‌ها را دنبال کنید.

---

## 📋 فهرست مراحل

1. دانلود و نصب XAMPP
2. کپی کردن فایل‌های سیستم TSR
3. راه‌اندازی سرویس‌های XAMPP
4. ایجاد پایگاه داده
5. تنظیم رمز عبور
6. ورود به سیستم
7. عیب‌یابی مشکلات رایج

---

## مرحله ۱: دانلود و نصب XAMPP

### ۱.۱ دانلود XAMPP

1. مرورگر خود (Chrome یا Firefox) را باز کنید
2. به آدرس زیر بروید:
   ```
   https://www.apachefriends.org/download.html
   ```
3. روی دکمه **"XAMPP for Windows"** کلیک کنید (اگر ویندوز دارید)
4. فایل دانلود می‌شود (حدود 150 مگابایت)

### ۱.۲ نصب XAMPP

1. روی فایل دانلود شده دابل‌کلیک کنید
2. اگر پیام امنیتی دیدید، روی **"Yes"** کلیک کنید
3. در پنجره نصب:
   - روی **"Next"** کلیک کنید
   - دوباره **"Next"** را بزنید
   - مسیر نصب را تغییر ندهید (پیش‌فرض: `C:\xampp`)
   - روی **"Next"** کلیک کنید
   - تیک همه گزینه‌ها را بزنید
   - روی **"Next"** و سپس **"Install"** کلیک کنید
4. صبر کنید تا نصب تمام شود (حدود ۵ دقیقه)
5. در پایان، تیک **"Launch XAMPP Control Panel"** را بزنید و **"Finish"** را کلیک کنید

---

## مرحله ۲: کپی کردن فایل‌های سیستم TSR

### ۲.۱ پیدا کردن پوشه htdocs

1. وارد مسیر زیر شوید:
   ```
   C:\xampp\htdocs
   ```
   (این پوشه‌ای است که XAMPP فایل‌های وبسایت را از آنجا می‌خواند)

### ۲.۲ کپی فایل‌ها

1. فایل‌های سیستم TSR را که در پوشه `/workspace/tsr_system/` دارید، کپی کنید
2. به پوشه `C:\xampp\htdocs` بروید
3. یک پوشه جدید بسازید به نام `tsr`
4. تمام فایل‌های سیستم TSR را داخل پوشه `tsr` کپی کنید

**ساختار نهایی باید اینگونه باشد:**
```
C:\xampp\htdocs\tsr\
├── config\
├── includes\
├── modules\
├── database\
├── assets\
├── templates\
├── uploads\
├── logs\
└── index.php
```

---

## مرحله ۳: راه‌اندازی سرویس‌های XAMPP

### ۳.۱ باز کردن کنترل پنل XAMPP

1. منوی استارت ویندوز را باز کنید
2. عبارت **"XAMPP Control Panel"** را جستجو کنید
3. روی آن کلیک کنید

### ۳.۲ شروع سرویس‌ها

در پنل XAMPP دو دکمه مهم می‌بینید:

| نام سرویس | توضیح | وضعیت مورد نیاز |
|-----------|-------|-----------------|
| **Apache** | سرور وب | باید سبز شود |
| **MySQL** | پایگاه داده | باید سبز شود |

1. روبروی **Apache** روی دکمه **"Start"** کلیک کنید
2. روبروی **MySQL** روی دکمه **"Start"** کلیک کنید
3. صبر کنید تا هر دو **سبز رنگ** شوند

✅ اگر سبز شدند، یعنی موفق شدید!
❌ اگر قرمز ماندند، به بخش "عیب‌یابی" در انتهای این راهنما مراجعه کنید.

---

## مرحله ۴: ایجاد پایگاه داده

### ۴.۱ باز کردن phpMyAdmin

1. مرورگر خود را باز کنید
2. در نوار آدرس تایپ کنید:
   ```
   http://localhost/phpmyadmin
   ```
3. Enter را بزنید
4. صفحه phpMyAdmin باز می‌شود

### ۴.۲ ایجاد پایگاه داده tsr_system

1. در سمت چپ صفحه، روی **"New"** (جدید) کلیک کنید
2. در کادر **"Database name"** بنویسید:
   ```
   tsr_system
   ```
3. روبروی **"Collation"** انتخاب کنید:
   ```
   utf8mb4_unicode_ci
   ```
4. روی دکمه **"Create"** کلیک کنید

✅ پیام موفقیت‌آمیز بودن عملیات را می‌بینید.

### ۴.۳ وارد کردن ساختار پایگاه داده

1. حالا روی نام پایگاه داده `tsr_system` در سمت چپ کلیک کنید
2. از تب‌های بالا، روی **"SQL"** کلیک کنید
3. یک کادر بزرگ متنی می‌بینید
4. حالا باید محتوای فایل `schema.sql` را کپی کنید:

**روش کپی کردن فایل schema.sql:**
   - به مسیر `C:\xampp\htdocs\tsr\database\` بروید
   - فایل `schema.sql` را با Notepad باز کنید
   - کل محتوا را انتخاب کنید (Ctrl+A)
   - کپی کنید (Ctrl+C)

5. برگردید به phpMyAdmin
6. متن کپی شده را در کادر SQL پیست کنید (Ctrl+V)
7. روی دکمه **"Go"** (اجرا) در پایین صفحه کلیک کنید

✅ اگر پیام‌های سبز رنگ موفقیت دیدید، یعنی پایگاه داده با موفقیت ساخته شد!

### ۴.۴ بررسی جداول ایجاد شده

1. در سمت چپ، روی `tsr_system` کلیک کنید
2. باید لیستی از جداول ببینید شامل:
   - users
   - job_groups
   - job_positions
   - workflow_states
   - workflow_transitions
   - tsr_requests
   - form_fields
   - attachments
   - permissions
   - role_permissions
   - system_settings
   - audit_log

اگر این جداول را می‌بینید، ✅ موفق شدید!

---

## مرحله ۵: تنظیم رمز عبور (اختیاری اما توصیه می‌شود)

### ۵.۱ چرا رمز عبور؟

به صورت پیش‌فرض، XAMPP رمز عبور ندارد که برای امنیت بهتر است یک رمز تنظیم کنید.

### ۵.۲ تنظیم رمز MySQL

1. در phpMyAdmin، از منوی بالا روی **"User accounts"** کلیک کنید
2. روبروی کاربر `root`، روی **"Edit privileges"** کلیک کنید
3. از منوی بالا روی **"Change password"** کلیک کنید
4. یک رمز عبور قوی وارد کنید (مثلاً: `TsrSystem123!`)
5. روی **"Go"** کلیک کنید

⚠️ **مهم:** حالا باید این رمز را در فایل تنظیمات سیستم هم وارد کنید:

1. به مسیر `C:\xampp\htdocs\tsr\config\` بروید
2. فایل `config.php` را با Notepad باز کنید
3. خط ۱۵ را پیدا کنید:
   ```php
   const DB_PASS = ''; // Change in production
   ```
4. آن را به این صورت تغییر دهید:
   ```php
   const DB_PASS = 'TsrSystem123!';
   ```
5. فایل را ذخیره کنید (File > Save)

---

## مرحله ۶: ورود به سیستم و اولین استفاده

### ۶.۱ باز کردن سیستم TSR

1. مرورگر خود را باز کنید
2. در نوار آدرس تایپ کنید:
   ```
   http://localhost/tsr
   ```
3. Enter را بزنید

### ۶.۲ صفحه ورود

اولین بار که وارد می‌شوید، صفحه ورود را می‌بینید.

**برای اولین ورود از اطلاعات زیر استفاده کنید:**

| فیلد | مقدار |
|------|-------|
| ایمیل | `admin@tsr.local` |
| رمز عبور | `Admin123!` |

⚠️ **توجه:** اگر این کاربران در پایگاه داده وجود ندارند، باید آنها را دستی اضافه کنید (به بخش "ایجاد کاربر اولیه" در پایین مراجعه کنید).

### ۶.۳ پس از ورود

بعد از ورود موفق، داشبورد سیستم را می‌بینید که شامل:

- 📊 آمار درخواست‌ها
- 📝 لیست درخواست‌های اخیر
- 🔄 نمودار گردش کار
- 👥 مدیریت کاربران

---

## 🎯 ایجاد کاربر اولیه (اگر لازم است)

اگر هنگام ورود با خطا مواجه شدید، یعنی کاربران اولیه در پایگاه داده وجود ندارند. برای ایجاد آنها:

### روش ۱: استفاده از phpMyAdmin

1. به `http://localhost/phpmyadmin` بروید
2. روی پایگاه داده `tsr_system` کلیک کنید
3. روی جدول `users` کلیک کنید
4. از تب بالا روی **"Insert"** کلیک کنید

**اطلاعات کاربر مدیر را وارد کنید:**

| فیلد | مقدار |
|------|-------|
| id | (خالی بگذارید - خودکار پر می‌شود) |
| personnel_code | `ADMIN001` |
| first_name | `مدیر` |
| last_name | `سیستم` |
| email | `admin@tsr.local` |
| password_hash | `$2y$12$LKqj5N9xQzJ5vZxR8mKpO.uWqY3hF6tG7dS8aP9bC0eD1fE2gH3iJ` |
| job_group_id | `5` |
| job_position_id | `4` |
| role | `admin` |
| is_active | `1` |

5. روی **"Go"** کلیک کنید

**رمز عبور هش شده بالا معادل `Admin123!` است.**

### روش ۲: اجرای مستقیم SQL

1. در phpMyAdmin، روی `tsr_system` کلیک کنید
2. روی تب **"SQL"** کلیک کنید
3. کد زیر را کپی و پیست کنید:

```sql
INSERT INTO `users` (`personnel_code`, `first_name`, `last_name`, `email`, `password_hash`, `job_group_id`, `job_position_id`, `role`, `is_active`) 
VALUES ('ADMIN001', 'مدیر', 'سیستم', 'admin@tsr.local', '$2y$12$LKqj5N9xQzJ5vZxR8mKpO.uWqY3hF6tG7dS8aP9bC0eD1fE2gH3iJ', 5, 4, 'admin', 1);
```

4. روی **"Go"** کلیک کنید

---

## 🔧 عیب‌یابی مشکلات رایج

### مشکل ۱: Apache سبز نمی‌شود

**علت:** پورت 80 توسط برنامه دیگری استفاده می‌شود (معمولاً Skype یا IIS)

**راه حل:**
1. در XAMPP Control Panel، روبروی Apache روی **"Config"** کلیک کنید
2. **"httpd.conf"** را انتخاب کنید
3. خطی که نوشته `Listen 80` را پیدا کنید
4. آن را به `Listen 8080` تغییر دهید
5. فایل را ذخیره کنید
6. حالا Apache را Start کنید
7. برای دسترسی به سیستم از آدرس زیر استفاده کنید:
   ```
   http://localhost:8080/tsr
   ```

### مشکل ۲: MySQL سبز نمی‌شود

**علت:** پورت 3306 مشغول است یا MySQL قبلاً نصب بوده

**راه حل:**
1. در XAMPP Control Panel، روبروی MySQL روی **"Config"** کلیک کنید
2. **"my.ini"** را انتخاب کنید
3. خطی که نوشته `port=3306` را پیدا کنید (دو جا هست)
4. هر دو را به `port=3307` تغییر دهید
5. فایل را ذخیره کنید
6. MySQL را Start کنید
7. حالا برای phpMyAdmin از آدرس زیر استفاده کنید:
   ```
   http://localhost:3307/phpmyadmin
   ```

### مشکل ۳: خطای اتصال به پایگاه داده

**علت:** رمز عبور یا نام پایگاه داده اشتباه است

**راه حل:**
1. مطمئن شوید پایگاه داده `tsr_system` را ساخته‌اید
2. فایل `C:\xampp\htdocs\tsr\config\config.php` را باز کنید
3. بررسی کنید:
   ```php
   const DB_HOST = 'localhost';
   const DB_NAME = 'tsr_system';
   const DB_USER = 'root';
   const DB_PASS = ''; // اگر رمز نگذاشته‌اید خالی باشد
   ```

### مشکل ۴: صفحه سفید یا خطای PHP

**علت:** خطای کدنویسی یا تنظیمات PHP

**راه حل:**
1. به پوشه `C:\xampp\htdocs\tsr\logs\` بروید
2. فایل `error.log` را با Notepad باز کنید
3. آخرین خطاهای ثبت شده را بخوانید
4. اگر خطای خاصی دیدید، آن را جستجو کنید

### مشکل ۵: فایل‌های CSS و JS لود نمی‌شوند

**علت:** مسیر فایل‌ها اشتباه است

**راه حل:**
1. مطمئن شوید پوشه `assets` در مسیر درست قرار دارد
2. بررسی کنید که فایل‌های CSS و JS وجود دارند
3. در مرورگر، کلید F12 را بزنید و تب Console را بررسی کنید

---

## 📞 پشتیبانی و کمک بیشتر

اگر با مشکلی مواجه شدید که در این راهنما نبود:

1. **بررسی لاگ‌ها:**
   - لاگ XAMPP: `C:\xampp\apache\logs\error.log`
   - لاگ سیستم: `C:\xampp\htdocs\tsr\logs\error.log`

2. **بررسی تنظیمات:**
   - مطمئن شوید Apache و MySQL هر دو سبز هستند
   - مطمئن شوید پایگاه داده `tsr_system` وجود دارد
   - مطمئن شوید جداول در پایگاه داده ساخته شده‌اند

3. **راه‌اندازی مجدد:**
   - در XAMPP Control Panel، روی **"Stop"** برای هر دو سرویس کلیک کنید
   - دوباره **"Start"** را بزنید

---

## ✅ چک‌لیست نهایی

قبل از استفاده از سیستم، این موارد را بررسی کنید:

- [ ] XAMPP نصب شده است
- [ ] Apache در XAMPP سبز است
- [ ] MySQL در XAMPP سبز است
- [ ] پایگاه داده `tsr_system` ساخته شده است
- [ ] تمام ۱۲ جدول در پایگاه داده وجود دارند
- [ ] فایل‌های TSR در `C:\xampp\htdocs\tsr\` کپی شده‌اند
- [ ] می‌توانید به `http://localhost/tsr` دسترسی داشته باشید
- [ ] کاربر admin را می‌توانید وارد کنید

---

## 🎉 تبریک!

اگر تمام مراحل را با موفقیت انجام دادید، اکنون سیستم TSR شما آماده استفاده است!

**آدرس‌های مهم:**

| کاربرد | آدرس |
|--------|------|
| صفحه اصلی سیستم | `http://localhost/tsr` |
| مدیریت پایگاه داده | `http://localhost/phpmyadmin` |
| کنترل پنل XAMPP | از منوی استارت |

---

## 📚 مستندات بیشتر

برای اطلاعات بیشتر درباره قابلیت‌های سیستم، نحوه کار با پنل مدیریت، و تنظیمات پیشرفته، فایل `README_FA.md` را در پوشه سیستم مطالعه کنید.

---

**تهیه شده برای کاربران غیرفنی - نسخه ۱.۰**
